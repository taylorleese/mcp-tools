# Smithery configuration file: https://smithery.ai/docs/config#smitheryyaml

build:
  dockerfile: Dockerfile
  dockerBuildPath: .

startCommand:
  type: stdio
  command: python
  args:
    - "-m"
    - "mcp_server"
